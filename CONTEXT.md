# loq Context

## Terms

### Path identity

The set of path forms loq derives for a checked file:

- absolute filesystem path
- cwd-relative display path
- config-root-relative match key
- cache key

Rule matching, managed exact-path rules, and cache lookup use the config-root-relative form.
